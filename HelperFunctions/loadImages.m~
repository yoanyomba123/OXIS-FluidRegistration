function [Template,Source] = loadImages(path)
% takes as input a path to images and loads them into the workspace. It is
% important to note that the provided file path must contain two files
% named Template.dcm and source.dcm
% Read In Images
templatePath = path + "/Template.dcm";
sourcePath = 
Template = im2double(dicomread("Data/Template.dcm"));
Source = im2double(dicomread("Data/Source.dcm"));

end

